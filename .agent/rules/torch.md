---
trigger: always_on
---

请为我设计并生成一套全面、系统、由浅入深的 PyTorch 学习内容，直接适配填充到教学/演示/知识管理界面中使用。内容必须以**官方文档（https://pytorch.org/）**、官方教程（https://pytorch.org/tutorials/）、官方文档（https://pytorch.org/docs/stable/）为最主要、最权威的依据，严禁凭空捏造API、机制、过时写法或不存在的功能。所有核心概念、API用法、最佳实践均应与当前稳定版（或注明版本）保持一致。
总体要求

内容深度与广度：从零基础开始，逐步推进到中高级乃至部分底层原理。宁可内容有一定冗余和重复强调，也绝不允许重要知识点缺失。同一个概念在初级、中级、进阶处可以多次出现，但侧重点和难度要递增。
讲解形式：
每个重要概念都要包含：清晰定义、为什么重要、与NumPy/其他框架的对比（若适用）、数学直观解释（必要时用LaTeX）、典型使用场景。
必须提供大量可直接运行的代码示例（包含import语句、完整小片段），并展示真实、可预期的输出（可以用文本形式呈现，或描述输出形状/数值特征）。
对于中高级、复杂、底层机制（如autograd求导过程、计算图构建与优化、内存管理、分布式、torch.compile、Dispatcher、C++扩展等），强烈建议使用交互式动画、动图、可视化示意图来进行讲解（例如：动态展示前向-反向传播过程、梯度累积、计算图节点依赖关系、CUDA流与并行、Kernel融合等）。如果当前界面支持嵌入动画/3D可视化/可交互组件，请优先采用此类形式。

结构要求：
首先生成一份非常详细的分级目录（建议采用 Chapter 0、Chapter 1 … 的形式），目录层级建议至少到三级（大章 → 小节 → 知识点/子主题）。
目录要体现渐进式难度递增：零基础 → 核心组件 → 模型构建 → 训练全流程 → 进阶技巧 → 生产级部署与优化 → 底层原理与生态。
目录完成后，一个章节一个章节地详细展开生成内容（不要一次性输出全部章节），每个章节内部建议再细分小节，并保持统一的讲解结构（概念 → 代码 → 输出 → 常见错误 → 扩展阅读）。

其他细节偏好：
优先使用现代写法（例如 torch.nn 模块、Dataset/DataLoader、nn.Module 子类化、torch.optim、torch.utils.tensorboard 等）。
涉及数据集时，优先使用官方推荐的（如Fashion-MNIST、CIFAR、ImageFolder等）或PyTorch自带示例数据。
鼓励在适当位置插入小练习题或思考点，帮助读者加深理解。
对于GPU、多卡、混合精度、torch.compile、torch dynamo、torch inductor 等现代加速技术，要有专门章节讲解，并尽可能展示前后性能对比。
语言风格：正式、严谨、条理清晰，像写给专业同事或研究生使用的内部培训教材。


请先输出详细的章节大纲（包含Chapter编号、标题、二级/三级子标题），待我确认或调整后再逐章详细展开内容。